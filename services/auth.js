const sessionId = new Map();

function setUser(id,user){
    sessionId.set(id,user);
}

function getUser(id,user){
    sessionId.set(id,user);
}

function setUser(id,user){
    sessionId.set(id,user);
}
